MenuBar
(타입) -> (solid <-> wire) : 도형의 면을 채울 것인지 채우지 않을 것인지
(투영) -> (평행 투영) : 원근감이 없는 투영 방식
	  -> (원근 투영) : 원근감이 있는 투영 방식

ToolBar
X -> 도형 모두 지우기
(육면체) -> 해당 버튼 클릭 후, 화면에 왼클릭 시 해당 위치에 육면체 생성
(원) -> 해당 버튼 클릭 후, 화면에 왼클릭 시 해당 위치에 구 생성
(도넛) -> 해당 버튼 클릭 후, 화면에 왼클릭 시 해당 위치에 원환체 생성

View
키보드 방향키 -> 현재 카메라가 바라보고 있는 방향을 기준으로 상하좌우로 카메라 이동
wasd -> 현재 카메라가 바라보고 있는 방향을 기준으로 상하좌우로 카메라 회전
(도형 우클릭) -> 해당 도형 선택, solid 상태일 때 선택 시 wire로, wire 상태일 때 선택 시 solid로 변경
(도형 우클릭된 상태에서 tfgh -> 현재 카메라가 바라보고 있는 방향을 기준으로 상하좌우로 도형 이동
(도형 우클릭된 상태에서 ijkl) -> 상하좌우로 도형 회전
